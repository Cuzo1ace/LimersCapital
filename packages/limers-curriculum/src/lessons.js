/**
 * Lesson content (keyed by lesson id).
 * See `./schema.js` for the Lesson type contract.
 * Pre-publication staging — see note in `./modules.js`.
 */
export { LESSONS } from '../../../src/data/lessons.js';
