node_modules/
dist/
.env
.env.local
.env.*.local
.vscode/
.DS_Store

# Remotion rendered video outputs (regeneratable, too large for git)
remotion/out/

# Anchor build artifacts + Solana program keypairs (NEVER commit)
anchor/target/
anchor/**/*-keypair.json
*.so

# Claude Code internal files
.claude/
.agents/
.vercel
