# Match the parent caribcryptomap repo: vite@8 + vite-plugin-pwa peer
# constraint forces legacy resolution. Without this, `npm install` fails
# with ERESOLVE between vite@8 and @vitejs/plugin-react's peer range.
legacy-peer-deps=true
